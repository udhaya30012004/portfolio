# Backend

This folder is reserved for server-side code, APIs, database access, authentication, and integrations.

The current backend is a minimal Node.js HTTP server with one health endpoint:

```bash
GET http://localhost:5000/health
```

## Commands

```bash
npm run dev
npm start
```
